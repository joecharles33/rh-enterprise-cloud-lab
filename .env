# --- Your workstation's public IP (no CIDR here) ---
PUBLIC_IP=35.134.187.98

# --- Terraform backend (already have these; keep for convenience) ---
AWS_REGION=us-east-1
TF_STATE_BUCKET=rh-enterprise-cloud-lab-tfstate-123456789012-dev
TF_LOCK_TABLE=rh-enterprise-cloud-lab-tf-locks

# --- EC2 key pair name (Terraform needs the name) ---
KEY_PAIR_NAME=a3-redhat-lab-key

# --- SSH private key path (Ansible will use this) ---
ANSIBLE_PRIVATE_KEY_FILE=~/.ssh/a3-redhat-lab-key.pem

PUBLIC_IP=35.134.187.98
